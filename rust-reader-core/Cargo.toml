[package]
name = "rust-reader-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Core domain models and reading logic for rustReader"

[dependencies]
serde = { workspace = true }
