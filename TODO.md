# rustReader 开发 TODO

按优先级分为 P0/P1/P2/P3。逐个实现，遇到不确定状况先询问用户。完整审计报告见 `docs/audit-report.md`。

## P0 — 影响基础可用性

- [x] 1. 书架封面缩略图
  - [x] 1.1 添加漫画/文件夹时异步提取第一页并生成封面缩略图
  - [x] 1.2 将封面路径写入 `LibraryEntry.cover_path` 并持久化
  - [x] 1.3 在书架 UI 中以卡片/网格形式展示封面 + 标题
  - [x] 1.4 封面缺失时显示占位色块
  - [ ] 1.5 历史/书签列表复用封面（后续 P1 统一处理）
- [x] 2. 统一 comic_id 生成
  - [x] 2.1 所有 parser 使用 `stable_comic_id(path)` 生成 ID
  - [x] 2.2 `ensure_in_library` 解析成功后使用 parser 生成的 ID
  - [x] 2.3 启动时迁移旧 library/history/bookmarks 到新的 path-based ID，并重命名封面文件
- [x] 3. 主题设置生效
  - [x] 3.1 在 `app.update` 中根据 `Settings.theme` 调用 `ctx.set_theme`
  - [x] 3.2 支持 System/Dark/Light 切换并即时生效
  - [x] 3.3 跟踪已应用主题避免每帧重复设置
- [x] 4. Webtoon 真正连续滚动
  - [x] 4.1 在 `reader.rs` 中新增 Webtoon 渲染分支
  - [x] 4.2 使用 `layout.rs` 的垂直布局计算每页偏移
  - [x] 4.3 滚轮改为垂直滚动，并自动更新当前页
  - [x] 4.4 键盘翻页后滚动到对应页面顶部
  - [x] 4.5 按需加载可见页缩略图/全图
- [x] 5. FitMode 与设置打通
  - [x] 5.1 删除 `QuickFit`，统一使用 `FitMode`
  - [x] 5.2 `ReaderView::open` 使用 `state.fit_mode` 作为初始适配
  - [x] 5.3 工具栏/快捷键 fit 操作使用 `FitMode`
  - [x] 5.4 打开漫画时从 `settings.default_fit` 设置 `state.fit_mode`
  - [x] 5.5 `apply_pending_fit` 同时更新 `state.fit_mode`

## P1 — 显著提升体验

- [ ] 6. 阅读器缩放/平移增强
  - [ ] 6.1 Ctrl/Command + 滚轮缩放
  - [ ] 6.2 双击切换 100% / 适应页面
  - [ ] 6.3 窗口 resize 时自动重新 fit
  - [ ] 6.4 限制平移边界，避免小图拖出视口
- [ ] 7. 缩略图失败提示与重试退避
  - [ ] 7.1 缩略图加载失败也进入错误状态或显示降级占位
  - [ ] 7.2 错误重试最多 3 次，带指数退避
  - [ ] 7.3 损坏页面显示“加载失败，点击重试”
- [ ] 8. PDF/RAR 缓存
  - [ ] 8.1 PDF：在 loader 中缓存已读文件字节或解析后的文档
  - [ ] 8.2 RAR：建立 `name -> header position` 索引，避免线性扫描
  - [ ] 8.3 关闭漫画或 epoch 变化时释放缓存
- [ ] 9. 书架/历史/书签右键菜单与元数据
  - [ ] 9.1 为书架条目添加右键菜单（打开/编辑/删除）
  - [ ] 9.2 显示页数、阅读百分比、添加时间
  - [ ] 9.3 历史/书签列表也支持右键操作
- [ ] 10. 进度条悬停缩略图保持比例
  - [ ] 10.1 按原图比例缩放预览图
  - [ ] 10.2 限制最大尺寸 80×120
- [ ] 11. 空书架引导
  - [ ] 11.1 空状态显示大大的“打开文件夹”按钮
  - [ ] 11.2 显示拖拽提示

## P2 — 功能完善

- [ ] 12. 书签 note 编辑
  - [ ] 12.1 添加书签时允许输入 note
  - [ ] 12.2 书签列表支持编辑/保存 note
- [ ] 13. 历史单条删除/清空
  - [ ] 13.1 历史列表支持单条删除
  - [ ] 13.2 提供“清空历史”按钮
- [ ] 14. 递归扫描导入
  - [ ] 14.1 添加文件夹时可选递归扫描子目录
  - [ ] 14.2 支持导入根目录下的多个漫画文件夹/压缩包
- [ ] 15. 跨页/宽页检测与显示选项
  - [ ] 15.1 检测明显横向长图并自动单页全宽显示
  - [ ] 15.2 提供“跨页”或“从右页开始双页”配置
- [ ] 16. 动画与当前 zoom/fit 状态一致
  - [ ] 16.1 让翻页动画使用当前 zoom/pan/fit 状态
  - [ ] 16.2 或提供“关闭动画”开关
- [ ] 17. 页面跳转输入框即时生效
  - [ ] 17.1 `DragValue` 在失去焦点或回车时跳转
- [ ] 18. 鼠标前进/后退键翻页
  - [ ] 18.1 在 `app.rs` 中处理额外鼠标按钮

## P3 — 工程精进

- [ ] 19. 上传纹理后释放 CPU 端 ColorImage
  - [ ] 19.1 `PageCache::get_texture` 上传后将 `image` 字段置空
  - [ ] 19.2 压缩模式保留压缩数据
- [ ] 20. protected_page_indices 改为 HashSet
- [ ] 21. SharedRawCache 锁粒度优化
  - [ ] 21.1 缩小 Mutex 持有范围
  - [ ] 21.2 评估使用并发缓存结构（如 dashmap）
- [ ] 22. 设置 JSON 原子写 + 备份 + 校验
  - [ ] 22.1 写入临时文件后 rename
  - [ ] 22.2 写入前备份旧文件
  - [ ] 22.3 加载错误时提示用户而非静默 fallback
  - [ ] 22.4 对 `decode_threads`、`cache_size_mb` 等做范围校验
- [ ] 23. 历史记录同时保存 comic_id 与 path
  - [ ] 23.1 修改 `HistoryEntry` 结构
  - [ ] 23.2 匹配时优先 comic_id，找不到则按 path 兜底
- [ ] 24. 项目文档与工程化
  - [ ] 24.1 添加 LICENSE 文件（MIT）
  - [ ] 24.2 添加 AGENTS.md
  - [ ] 24.3 添加 GitHub Actions CI（fmt/clippy/test）
  - [ ] 24.4 添加 CHANGELOG.md
  - [ ] 24.5 清理/更新 docs/ 中过时的设计文档
- [ ] 25. 增加非 GUI 集成测试
  - [ ] 25.1 parser 多格式 round-trip 测试
  - [ ] 25.2 storage 文件 I/O 测试
  - [ ] 25.3 loader 并发与缓存行为测试

## 历史已完成项

- [x] 修复双页模式下右页无右键/拖拽响应
- [x] 解码/加载失败时给出用户可见的错误提示（占位图 + 文字）
- [x] 打开大文件时显示 loading 状态（解析阶段目前是同步的）
- [x] 验证并确保退出时阅读位置能正确恢复
- [x] 全屏时自动隐藏工具栏/进度条
- [x] 书签列表和历史列表 UI
- [x] 书架支持删除/编辑条目
- [x] 图片降采样，避免超高分辨率图撑爆内存
- [x] 点击屏幕左右半边翻页
- [x] 翻页动画
- [x] 封面单页处理（双页模式）
- [x] 更多设置项（背景色、默认模式、默认缩放）
- [x] 自定义快捷键
- [x] 书架搜索/排序
- [x] 多线程解码池
- [x] 压缩包索引缓存
- [x] GPU 纹理压缩（DXT5/BC3 作为 CPU 压缩缓存，按需解压为 egui 托管纹理）
- [x] 修复打开特定 webp 压缩包时卡死（改用 RangeFit 压缩 + 解码队列优先级）
- [x] macOS 使用 wgpu/Metal 后端渲染，减少 CPU 占用
- [x] 默认使用原图展示，并在设置中增加 DXT5 压缩开关
- [x] 修复翻页后偶尔一直显示“加载中”的问题（可见页请求重试 + 无界结果队列）
- [x] 修复 zip 中扩展名与实际格式不符的图片解码失败（自动回退 magic 检测）
- [x] 缩略图优先渲染管线（打开时后台生成缩略图，真实图片按需预读）
- [x] 优化缩略图生成性能（macOS ImageIO 快速路径 + 快速缩放算法 + PDF 低分辨率渲染）
- [x] macOS 缩略图使用 `CGImageSourceCreateThumbnailAtIndex` 直接生成，避免解码全图
- [x] IO worker 间共享解压后的压缩包原始数据缓存，避免同一页被重复解压
- [x] 缩略图和全图使用独立的低优先级解码队列，防止全图预加载阻塞缩略图
- [x] 根据缓存预算动态限制全图预读窗口，避免缓存反复抖动导致 CPU 持续 100%
- [x] 翻页时若目标页已缓存全图则直接展示原图，而不是缩略图
- [x] 默认真实图片缓存页数调整为 10
- [x] 方向感知非对称预读：Ltr 优先向前预读，Rtl 优先向后预读
- [x] 清理历史 clippy 警告
