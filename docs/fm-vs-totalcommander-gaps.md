# 文件管理器 vs Total Commander 差距清单

对比基准：Total Commander 10/11（默认配置 + 常见使用习惯）。
现状来源：`openitgo-app/src/views/file_manager*.rs`、`file_ops.rs`、
`preview_bytes.rs`（2026-02 盘点）。按「功能 / 操作 / 外观 / 使用习惯」四维度列出，
每项标注差距性质：**缺失**（完全没有）/ **弱化**（有但不完整）/ **有意不同**（设计取舍）。

> 注：§一–§四正文表格保留 2026-02 盘点原貌不再逐行修订；其后的迭代实现
> （阶段 A–N，对应 commit `567fb90`–`58d2b7f`）以 §五 各优先级条目上的
> ✅ 标注为准。

## 一、功能差距

### 1. 栏与窗口结构

| TC | 现状 | 性质 |
|---|---|---|
| 栏内**标签页**（Ctrl+T 新建、Ctrl+W 关闭、Ctrl+Tab 切换、锁定标签、标签重命名、保存标签组） | 无标签页，每栏仅一个目录 | 缺失，大差距 |
| 底部**命令行输入条**（可直接执行命令、Ctrl+P 把当前路径送进去、Ctrl+Enter 把选中文件名送进去） | ✅ 阶段 X：`fm_command_bar`（默认开），底部单行命令条（状态栏上方），Enter 后台执行（cmd /c / sh -c，工作目录 = 焦点栏目录）、↑/↓ 会话内历史回填、Esc 清空交还焦点、Ctrl+P/Ctrl+Enter 送路径/文件名 | 已对齐（无按钮化、无输出捕获，超出范围） |
| 顶部**按钮栏**（用户自定义按钮：程序、命令、菜单） | ✅ 阶段 Y：`fm_button_bar` 自定义按钮栏（顶栏下方按钮条；%P/%N/%p 占位符；设置页列表编辑 + 运行时右键编辑/删除，两处互通） | 已对齐（无菜单型按钮，超出范围） |
| 两个独立**驱动器按钮栏**（每栏一排驱动器按钮，Alt+F1/F2 弹驱动器菜单） | 面包屑最左一个盘符下拉（两栏各自有） | 弱化，够用 |
| 树形面板模式（Alt+F10，一栏变目录树） | 无 | 缺失 |
| Ctrl+Q **快速查看**：另一栏整体变成预览面板（双栏下的常驻预览） | 预览仅存在于单栏模式右半 + 双栏 F3 临时弹窗 | 弱化 |

### 2. 显示模式

| TC | 现状 | 性质 |
|---|---|---|
| Brief（仅名称多列）/ Full（详细列）/ **缩略图视图** | ✅ 阶段 V：三态（列表/简表/缩略图）顶栏菜单切换；简表 = 多列图标+截断名称（行主序，键盘/框选/空白双击与网格共用机制） | 已对齐（简表为行主序，TC 为列主序） |
| **分支视图**（Ctrl+B：当前目录+所有子目录文件扁平列出） | ✅ 阶段 E：Ctrl+B 递归扁平列举（上限截断状态栏提示，面包屑 [分支] 标记） | 已对齐 |
| **自定义列视图**（可增删列、按属性/插件字段配置多套列布局） | ✅ 阶段 V：列头右键勾选增删 Ext/Size/Mtime/Attr/Comment 列（Name 恒首列弹性，固定列 ≥1），`fm_columns` 持久化 | 弱化（单套列配置，无多套列布局/插件字段） |
| 按文件类型**着色规则**（如 .exe 红色、临时文件灰色） | 无（仅图标区分类型） | 缺失 |
| 文件**注释**列（descript.ion） | ✅ 阶段 W：`fm_comments.rs` 读/写 descript.ion（引号名、编码探测、保序重写、清空删文件）；Comment 列接数据 + 悬停 tooltip 追加注释 + 右键「编辑注释…」/Ctrl+Shift+Z 对话框编辑 | 已对齐（分支视图子目录项不读各自目录注释；Ctrl+Z 预留撤销故用 Ctrl+Shift+Z） |
| 显示系统**真实文件图标**（exe 内嵌图标、图片缩略图图标） | ✅ 阶段 S：SHGetFileInfoW 系统图标（列表 16pt/网格 32pt 档，`fm_system_icons` 可关，Windows）；图片网格另走缩略图 | 已对齐（Windows；非 Windows 字体图标） |

### 3. 选择与过滤

| TC | 现状 | 性质 |
|---|---|---|
| Num+ / Num− **通配符选择/取消选择组**（如 `*.zip`、`EP*`） | 无，过滤框只支持子串匹配、不支持通配符 | 缺失 |
| Num* **反选** | 无反选 | 缺失 |
| 灰色 +/− 仅选择/取消**文件**（不动目录） | Ctrl+A 全选含目录，无「只选文件」变体 | 缺失 |
| Insert / 空格 **勾选当前项并下移**（可只勾不选，红名标记选中集） | 空格 = 计算目录大小；勾选只有 Ctrl+单击 / Ctrl+方向键+无空格配合 | 弱化 |
| **快速搜索**（字母键直接跳到匹配项 / Ctrl+S 增量搜索框 / Alt+字母仅过滤显示） | ✅ 字母 type-ahead 定位（阶段 A）+ Ctrl+S 聚焦过滤框、已聚焦选中全文（阶段 T） | 已对齐（无 Alt+字母仅过滤变体） |
| 过滤支持**通配符与正则**、可保存过滤方案 | ✅ 阶段 T：含 `*`/`?` 走通配（`;` 分隔多模式）+ 保存方案下拉 + 会话历史最近 8 条 | 弱化（无正则） |
| 鼠标**框选**（可配置右键拖动框选） | ✅ 阶段 U：`fm_rubber_band` = 右键拖动（默认）/左键空白起拖/关闭，替换/Shift 追加/Ctrl 切换 | 已对齐 |
| 保存/恢复选择集 | ✅ 阶段 U：右键「选择」子菜单（会话内命名保存/恢复/删除，恢复按路径匹配当前栏，缺失项提示） | 弱化（会话内不落盘） |

### 4. 排序

| TC | 现状 | 性质 |
|---|---|---|
| 更多排序键：扩展名、属性、无排序（物理顺序） | ✅ 阶段 V：`SortKey::Unsorted`（物理序，asc=false 反向）+ `SortKey::Attr`（R/H/S 属性串，无属性垫底）+ 既有名称/大小/时间/扩展名；列头右键菜单直达 | 已对齐 |
| 每目录记忆排序（可配） | 全局单一排序（仅活动栏持久化） | 弱化 |
| 目录置顶可开关 | 目录恒置顶（不可关） | 有意不同 |

### 5. 文件操作引擎

| TC | 现状 | 性质 |
|---|---|---|
| **执行期逐个冲突询问**（覆盖/跳过/重命名/追加，带缩略图对比、全部应用选项） | 策略操作前一次性确定；「询问」档执行期按跳过处理 | 有意不同（简化），但「询问」语义被吞 |
| 复制进度窗：**速度、剩余时间、独立窗口、可暂停**、后台化（F2 放后台继续操作） | 状态栏进度条 + 速度/ETA + 暂停/取消已具备；✅ 阶段 AA：独立任务面板窗口（顶栏「任务」/点击进度区打开，列出全部在途+排队任务，逐任务暂停/取消） | 已对齐 |
| **传输队列**（多任务排队、队列管理器） | ✅ 阶段 AA：`fm_op_threads` 并发上限（默认 2，0 = 不限，≤8），超限任务 FIFO 排队、在途结束自动放行，排队可取消；任务面板即队列管理器 | 已对齐（无手动调整队序） |
| 复制后**校验**（写入后重读比对） | ✅ 阶段 AB：确认框「复制完成后校验」checkbox（默认关），worker 复制后逐文件 `files_identical` 分块比对（长度快查 + 第二轮进度），不一致记 errors「校验失败」 | 已对齐 |
| 分割/合并文件 | ✅ 阶段 AD：右键「分割…」（单文件，按大小/份数，`name.NNN` 产出，已存在分块整批不覆盖）/「合并…」（单个 .001 或一组同前缀分块，缺号报错；同目录 .crc 存在时顺带 CRC32 校验），均走 OpKind 任务队列 | 已对齐 |
| 校验和计算/校验（CRC32/MD5/SHA） | ✅ 阶段 AC：右键「校验和…」非模态对话框——后台分块同算 CRC32/SHA-1/SHA-256（依赖树已有 crate 复用；MD5 无依赖不引入），点击行复制 hex；「验证」模式解析 md5sum/sfv 校验文件逐条标 ✓/✗/缺失/不支持 | 已对齐（缺 MD5） |
| 修改属性/时间戳 | ✅ 阶段 AC：右键「修改属性/时间戳…」——只读/隐藏/系统/存档位（Windows SetFileAttributesW，unix 仅只读位）+ 修改/访问时间设为指定时间或当前时间（filetime） | 已对齐 |
| **多格式打包**（zip/tar/gz + 插件 rar/7z；Alt+F5） | 仅写出 zip（引擎在 parser，支持读多格式） | 弱化 |
| 撤销（undo） | 无（删除走回收站可挽回，复制/移动不可撤销） | 缺失 |

### 6. 工具类功能

| TC | 现状 | 性质 |
|---|---|---|
| **批量重命名**（Ctrl+M：搜索替换/计数器/日期/正则，实时预览） | 单项重命名 | 缺失，大差距 |
| 文件**搜索器**（Alt+F7：按名称/内容/大小/日期，结果可「输送到文件列表」） | 仅当前目录子串过滤 | 缺失 |
| **同步目录**（两侧递归对比、按差异复制） | ✅ 阶段 AE：顶栏/右键「同步目录…」（双栏且目录不同）——后台递归对比（仅左/仅右/左新/右新/相同，≤2s mtime 容差），三方向 + 可选删除多余项，执行走 Copy Overwrite/Delete 任务队列 | 已对齐（无子目录勾选/按内容对比） |
| 按内容**比较两个文件** | ✅ 阶段 AF：右键「比较内容…」（选中集恰 2 文件）/「比较两栏焦点文件」（双栏两焦点文件）——后台 worker 二进制快比（大小不等直接结论，否则 256KB 分块报首差异偏移）+ 文本判定（扩展名或 8KB 无 NUL 嗅探）且 ≤64MB 时行级 diff（自实现 LCS，行数乘积超阈值回退仅二进制），单滚动区左右对照（删红/增绿） | 已对齐（无二进制 hex 对照/编辑合并） |
| Alt+Enter 系统**属性对话框** | 无 | 缺失 |
| 「打开方式…」菜单 | 双击直走 open_path 内部分发，无系统打开方式选择 | 缺失 |
| 新建**文件**（Shift+F4 或右键新建文本文件） | 仅新建文件夹 | 缺失 |
| 在当前目录打开**命令行/终端** | 无 | 缺失 |
| 计算选中项**总大小**显示在状态栏（Ctrl+L） | 状态栏只有「共 N 项 / 已选 M 项」，无字节合计；目录大小要手动逐项算 | 弱化 |
| 状态栏显示驱动器**剩余空间** | ✅ 阶段 Q：`剩余 X GB`（路径前弱色，30s 缓存，Windows GetDiskFreeSpaceExW / unix statvfs） | 已对齐 |
| 状态栏**过滤状态**指示 | ✅ 阶段 Q：过滤非空时状态栏弱色 `过滤: xxx`（与 `定位:` 并列） | 已对齐 |

### 7. 预览器（Lister 对比）

| TC Lister | 现状 F3/单栏预览 | 性质 |
|---|---|---|
| 图片/文本/十六进制/多媒体（插件）四种模式 | ✅ 阶段 R：文本/图片/HEX 三模式 tab（多媒体仍走插件外）；二进制类型也可 HEX 查看 | 弱化（无多媒体模式） |
| 文本编码**手动切换**、自动检测可配 | ✅ 阶段 R：编码下拉（自动/UTF-8/GBK/SJIS/Big5），重解码不重读文件，换目标回自动 | 已对齐 |
| 图片**缩放/旋转/全屏**、多帧 | ✅ 阶段 R：适应宽度/原始尺寸 + 显示层旋转 90°（不写文件）+ F3 弹窗最大化；无多帧 | 弱化（无多帧） |
| 文本内**搜索**（F7 在 Lister 内查找） | ✅ 阶段 R：搜索框 + ◀▶ + 计数 + 行高亮/滚动揭示（不区分大小写） | 已对齐 |
| 跟随模式（F3 后按 n/p 在文件间翻预览不关闭） | 移动焦点内容跟随（等价，已具备） | 对齐 |

### 8. 拖放与剪贴板互通

| TC | 现状 | 性质 |
|---|---|---|
| **从 Explorer/桌面拖入** FM 复制/移动 | ✅ 阶段 I：egui `dropped_files` 经 `panel_rect_at` 判定落点栏，弹复制确认框 | 已对齐（仅复制，无拖入移动） |
| **拖出到 Explorer/桌面**（OLE HDROP） | ✅ 阶段 I：FM 行拖出（位移 >40pt 且出窗触发，Windows OLE）；✅ 阶段 Z：按住 Shift 允许 MOVE，落点实际移动成功后源经 file_ops Delete 任务进回收站 | 已对齐（macOS/Linux 无 OLE 拖出） |
| 栏间拖动**默认直接复制**（拖动时按住 Shift=移动，光标有 +/箭头区分），无确认 | ✅ 阶段 I：双栏接收，松开弹确认框（对齐 F5 语义，徽标提示「N 项」）；`fm_drag_confirm` 关闭后直拷自动改名且 Shift=移动（徽标提示「N 项 · 移动」） | 已对齐（默认更保守，可关确认） |
| 拖到面包屑/驱动器按钮/标签上复制 | ✅ 阶段 Z：面包屑段与非当前标签均为落点（rect 渲染时记录，单/双栏通用），松开按 `fm_drag_confirm` 弹框或直拷 | 已对齐（驱动器按钮落点不做） |
| Ctrl+C/X/V 与**系统剪贴板互通**（HDROP，可与 Explorer 互贴） | ✅ 阶段 I：Windows HDROP 复制/剪切/粘贴（写失败回退应用内）；✅ 阶段 Z：macOS NSPasteboard 文件列表（`writeObjects:`/`readObjectsForClasses:`，恒复制语义） | 已对齐（Linux 维持应用内剪贴板） |

### 9. 导航习惯

| TC | 现状 | 性质 |
|---|---|---|
| **目录历史下拉**（Alt+↓ 弹出本栏去过的目录列表） | 只有 ‹ › 逐条后退/前进，无历史列表 | 缺失 |
| **目录热键表**（Ctrl+D：多级菜单、可分组、加注释、「添加当前目录」含子目录选项） | ✅ 阶段 P：Ctrl+D 开/关 ★ 书签菜单（阶段 N 已有分组子菜单；无注释/含子目录选项） | 弱化（无注释/子目录添加选项） |
| Ctrl+\ 回**根目录** | 无快捷键（面包屑段可点回，盘符根行内「..」禁用） | 缺失 |
| Ctrl+←/→ 把本栏目录**复制到另一栏**（或打开选中目录到另一栏） | 无 | 缺失 |
| Ctrl+U **交换两栏** | 无 | 缺失 |
| 面包屑可**直接输入路径**（TC 是命令行条承担） | 面包屑只读分段点击 | 缺失 |
| 配置「 Enter 在压缩包上进档内浏览」 | 压缩包一律跳 Archive 视图（决策 3） | 有意不同 |
| 双击空白处回上级（可配） | ✅ 阶段 P：列表/网格空白区双击 → 上级（`fm_dblclick_blank_up` 默认 true） | 已对齐 |

### 10. 键盘映射（TC 默认，现状缺失部分）

| 键位 | TC 行为 | 现状 |
|---|---|---|
| F4 | 编辑（系统/内置编辑器） | ✅ 阶段 P：系统「编辑」动词，无关联回退「打开方式…」（Windows） |
| Shift+F4 | 新建文本文件并编辑 | ✅ 阶段 M：新建文本文件（不含「并编辑」） |
| Alt+F5 | 压缩打包 | ✅ 阶段 P：压缩为 zip 对话框（同右键） |
| Alt+F9 | 解压 | ✅ 阶段 P：解压对话框（单压缩包选中集，同右键） |
| Alt+Enter | 属性 | 无 |
| Alt+F7 | 搜索 | 无 |
| Ctrl+M | 批量重命名 | 无 |
| Ctrl+B | 分支视图 | 无 |
| Ctrl+Q | 快速查看面板 | 无 |
| Ctrl+D | 目录热键表 | ✅ 阶段 P：开/关 ★ 书签菜单 |
| Ctrl+L | 计算选中大小 | ✅ 阶段 P：选中集目录批量计算大小（无选中回退焦点目录） |
| Ctrl+\ | 根目录 | 无 |
| Ctrl+U | 交换两栏 | 无 |
| Ctrl+←/→ | 目录传给另一栏 | 无 |
| Insert / 空格 | 勾选当前并下移 | 空格=目录大小；无 Insert |
| Num+ / − / * | 选择组/取消/反选 | 无 |
| Ctrl+T / Ctrl+W | 标签页 | 无 |
| F5 刷新 | （TC 无，Explorer 习惯） | Ctrl+R 刷新（TC 用户顺手，Explorer 用户会按 F5 落空） |
| Ctrl+Shift+Enter | 以管理员运行 | ✅ 阶段 P：runas 动词（Windows） |

已对齐的键位：Tab、↑↓/Home/End/PgUp/PgDn、Enter、Backspace、Alt+←/→、
Ctrl+A、F2 重命名（TC 是 Shift+F6，F2 属 Explorer 习惯）、F3 查看、F5 复制、
F6 移动、F7 新建文件夹、F8/Del 删除。

## 二、操作习惯差距

1. **选中即复制目标**：TC 无选中集时对焦点项操作——已对齐（`op_targets` 回退焦点项）。
2. **确认习惯**：TC F5/F6 默认弹一次确认框（含目标路径可改、仅复制所选类型过滤、
   仅复制指定日期后文件等扩展选项），执行中冲突再问。✅ 阶段 AB：确认框增
   「高级」折叠区（仅复制匹配通配符 + 仅最近 N 天，预扫描/执行共用
   `copy_filter_matches`，目录保留只过滤文件、total 按过滤后集合；Move 开过滤
   时禁用 rename 快速路径、逐文件部分搬运）。执行中冲突再问仍属有意不同（见 §5）。
3. **进度反馈粒度**：TC 显示文件名 + 总进度 + 单文件进度 + 速度 + ETA；
   现状只有总百分比 + 当前文件名，无单文件进度（大文件复制时进度条长时间不动）。
4. **删除习惯**：TC 默认 F8 物理删除、Shift+Del 直删可选、Del 到回收站（可配）；
   现状一律回收站（更安全，属于有意不同）。
5. **移动语义**：TC 跨盘移动成功后源物理删除；现状源进回收站（更安全，有意不同）。
6. **焦点跟随**：TC 操作完成后焦点落在下一项 / 新项；现状重命名/新建后选中新项
   （已对齐），复制/移动后焦点行为未定义（依赖刷新保留选中）。
7. **刷新时机**：TC 依赖 FS watch + 手动 Ctrl+R（F5 是复制）；现状 watch
   300ms 去抖自动刷新（已对齐并更好），默认非递归只盯当前层；✅ 阶段 Z：
   `fm_watch_recursive` 可选递归监听（子目录内变化也触发刷新；大目录/网络盘
   开销大，默认关）。
8. **错误报告**：✅ 阶段 AA：操作结束含失败项时除 toast（前 3 项）外弹非模态
   错误汇总窗（虚拟化列出全部失败项），「重试失败项」按原参数重建任务
   （只纳入仍存在的源）。
9. **后台继续工作**：TC 复制可放后台，继续浏览操作；现状操作线程后台跑、UI 不阻塞
   （已对齐）；✅ 阶段 AA：任务面板可查看/管理全部在途与排队任务。

## 三、外观差距

1. **图标**：✅ 阶段 S（Windows）：列表/网格显示系统真实图标（exe/lnk/ico 按
   文件取内嵌图标，目录/扩展名走 Shell 图标缓存免读盘）；非 Windows 仍为字体
   图标。
2. **缩略图模式**：无（对漫画阅读器用户的图片目录价值最高）。
3. **颜色定制**：TC 可按文件类型/属性定义前景色，整体主题可换肤；现状跟随
   egui 全局主题，FM 无独立配色入口。
4. **字体/行高可配**：TC 可调列表字体字号行距；现状固定（行高 22pt 与全局缩放走）。
5. **列布局持久化**：✅ 阶段 V：`fm_columns` 持久化列集合与列宽（全局单值取
   活动栏）；每标签列/排序/视图模式仅会话内（PanelTabSnapshot），不落盘。
6. **状态栏信息密度**：TC 显示选中数/总大小/驱动器剩余空间/过滤状态；
   现状只有数量与路径。
7. **选中视觉**：TC 选中项文字变红（经典配色）+ 反色条；现状底色高亮 + 焦点描边
   （视觉已足够，但「勾选但非焦点」与「焦点但未勾选」的区分不如 TC 红字直观）。
8. **双栏方向**：TC 左右栏（可 Alt+方向键操作对面栏）；现状一致。
9. **窗口标题**：TC 显示当前路径；现状按 `sync_window_title` 规则（FM 未单独定制）。

## 四、与 TC 习惯相反或容易误操作的点（迁移成本）

1. **空格语义**：TC 默认空格 = 勾选文件并下移（在目录上才是算大小）；现状空格
   恒为目录大小计算，文件上无行为。TC 老用户会误算。
2. **Insert 勾选**：完全缺失，TC 用户的「空格/Ins 一路勾过去」肌肉记忆无处安放。
3. **F5 刷新错觉**：Explorer 用户按 F5 想刷新会触发复制对话框。
4. **字母定位**：TC/Explorer 都支持按首字母跳文件，现状无——长目录只能滚动或
   点过滤框，键盘流断裂感最强的一点。
5. **拖动即复制**：TC 拖动直拷，现状拖动必弹确认框——更安全但老用户嫌啰嗦。
6. **右键菜单项顺序**与 TC 差异大（现状对齐 Explorer 风格）；TC 用户找
   「压缩/解压」在 Alt+F5/F9 键位而非右键。
7. **过滤框位置**：✅ 阶段 T：`fm_filter_bar_bottom` 可切到焦点栏底部就近输入，
   Ctrl+S 直达聚焦；Esc 语义（过滤框内清空交还焦点 → 清过滤→清选择）与 TC
   （Esc 关面板）仍有意不同。

## 五、建议的优先级（按使用频率 × 实现成本）

**P0（高频刚需，成本中低）**
- ✅ 字母快速定位 / 增量搜索（type-to-select）（阶段 B：`Event::Text` 捕获进
  type-ahead 缓冲，命中即焦点+单选）
- ✅ Num* 反选、Num+/− 通配符选择组；「仅选文件」变体（阶段 A：egui 无小键盘
  键位，主键盘 `*`/`+`/`-` 经 `Event::Text` 捕获；选择组对话框含「仅选文件」）
- ✅ 按扩展名排序键（阶段 A：`SortKey::Ext`）
- ✅ 状态栏显示选中项总大小（阶段 A：文件直接求和；目录仅计入「计算大小」
  已缓存的值，不触发新计算）
- ✅ Ctrl+U 交换两栏、Ctrl+←/→ 目录传给另一栏、Ctrl+\ 根目录（阶段 B）
- ✅ 目录历史下拉（Alt+↓）（阶段 B）

**P1（价值高，成本中）**
- ✅ 缩略图视图（阶段 H：`ThumbCache` 两栏共享 + 可见范围代次节流；
  选择/过滤/键盘与列表模式完全共用）
- ✅ 栏内标签页（阶段 G：Ctrl+T 新建 / Ctrl+W 关闭 / Ctrl(+Shift)+Tab 循环，
  目录与活动索引持久化；TC 的锁定/重命名/标签组未做）
- ✅ 系统剪贴板互通（HDROP 复制/粘贴）+ 拖入/拖出 OLE（阶段 I：写系统剪贴板
  失败回退应用内；拖入经 `handle_dropped_files` 落点栏判定；拖出复用
  `drag_out` OLE HDROP）
- ✅ 批量重命名（阶段 F：Ctrl+M，纯函数计划 + 实时预览，逐项失败汇总）
- ✅ 进度条加单文件进度 + 速度/ETA（阶段 C：另有暂停/继续）
- ✅ Ctrl+Q 对面栏快速预览（阶段 D：双栏下非活动栏整栏替换为预览，焦点跟随）
- ✅ 分支视图（阶段 E：Ctrl+B，截断上限状态栏提示，面包屑 [分支] 标记）

**P2（锦上添花）**
- ✅ 搜索器（阶段 J：Alt+F7，名称通配/大小区间/日期/内容，结果「输送到焦点栏」
  分支视图 + 双击 reveal 跳所在目录）
- ✅ 系统属性对话框（Alt+Enter）/「打开方式…」（阶段 M：Windows shell 动词，
  非 Windows 隐藏）
- ✅ 新建文本文件（阶段 M：Shift+F4 / 右键，重名自动递增建议名）
- ✅ 面包屑可编辑路径输入（阶段 M：铅笔按钮 → TextEdit，Enter 导航 / Esc 还原 /
  无效红字保持编辑）
- ✅ 执行期冲突逐个询问（阶段 K：「逐个询问」档执行期真正逐条问，含双方大小/
  时间快照与「全部应用」；无缩略图对比）
- ✅ 目录热键表分组（阶段 N：★ 书签分组子菜单 + 新建/重命名/删除分组，
  旧扁平书签自动迁移进「常用」组）
- ✅ 列宽/col_shift 持久化 + 名称语义着色（阶段 L：`fm_col_*` 全局单值取
  活动栏；目录 accent / 符号链接斜体 / 隐藏弱档，固定规则不做用户配色）
- ✅ 键位补齐（阶段 P：F4 编辑 / Ctrl+D 书签菜单 / Ctrl+L 批量目录大小 /
  Alt+F5 压缩 / Alt+F9 解压 / Ctrl+Shift+Enter 以管理员运行 /
  双击空白回上级 `fm_dblclick_blank_up`）
- ✅ 状态栏驱动器剩余空间 + 过滤状态指示（阶段 Q：`platform::drive_info`，
  30s 缓存切卷即重查；`过滤: xxx` 与 `定位:` 并列）
- ✅ 预览器增强（阶段 R：文本/图片/HEX 三模式 tab + 编码手动切换 +
  文本内搜索高亮 + 图片显示层旋转 + F3 弹窗最大化；二进制类型 HEX 可查看）
- ✅ 系统真实图标（阶段 S，Windows：`platform::file_icons` SHGetFileInfoW +
  `SysIconCache` 后台加载，列表 16pt/网格 32pt 档，exe/lnk/ico 按路径、
  其余按扩展名缓存，`fm_system_icons` 可关）
- ✅ 过滤增强（阶段 T：含 `*`/`?` 走 `wildcard_match` 通配、保存方案
  `fm_saved_filters` + 会话历史最近 8 条、Ctrl+S 聚焦过滤框、
  `fm_filter_bar_bottom` 底栏模式、过滤框 Esc 清空交还焦点）
- ✅ 选择增强（阶段 U：鼠标框选 `fm_rubber_band` 右键/左键空白/关闭 +
  替换·Shift 追加·Ctrl 切换；右键「选择」子菜单保存/恢复选择集，
  会话内不落盘、恢复时缺失项提示）
- ✅ 显示模式与自定义列（阶段 V：三态视图 列表/简表/缩略图；简表 =
  多列图标+截断名称行，行主序与网格共用键盘/框选/空白双击机制；
  列头右键勾选增删 Ext/Size/Mtime/Attr/Comment 列，`fm_columns` 持久化，
  旧档由 `fm_col_*` 播种默认三列；`SortKey::Unsorted`/`Attr` 排序键；
  标签快照补齐排序/列/视图模式）
- ✅ 文件注释（阶段 W：descript.ion 读/写——引号名、chardetng 编码
  探测、保序重写、清空删文件、UTF-8 无 BOM；Comment 列接数据 +
  悬停 tooltip 追加注释行 + 右键「编辑注释…」/Ctrl+Shift+Z 对话框，
  列举就绪时读入面板缓存、FS watch 变更天然触发重读）
- ✅ 命令行输入条（阶段 X：`fm_command_bar` 默认开；底部状态栏上方单行
  命令条，弱色 `{dir}>` 前缀；Enter 经 cmd /c / sh -c 后台执行
  （工作目录 = 焦点栏目录，失败上报 error_message），↑/↓ 会话内历史
  回填（去重置顶、上限 32），Esc 清空交还焦点，Ctrl+P 送当前路径、
  Ctrl+Enter 送焦点项文件名）
- ✅ 自定义按钮栏（阶段 Y：`fm_button_bar` 按钮列表持久化；顶栏下方
  按钮条（空 = 不渲染，占位「+ 添加按钮」在顶栏），点击后台执行
  （%P 当前目录 / %N 焦点项名称 / %p 另一栏目录占位符），悬停 tooltip
  或展开命令，右键「编辑…/删除」；设置页「按钮栏」子区块列表编辑
  （↑↓ 移序 / ✕ 删除 / 三输入添加），两处改动互通互不覆盖）
- ✅ 拖放/剪贴板/watch 收尾（阶段 Z：面包屑段/非当前标签成为拖放复制
  落点（单/双栏通用）；FM 拖出按住 Shift 允许 OLE MOVE、落点实际移动
  成功后源进回收站；macOS NSPasteboard 文件列表剪贴板（恒复制语义，
  未经本地编译验证、依赖 CI/真机）；`fm_watch_recursive` 可选递归目录
  watch，默认关）
- ✅ 任务队列 + 错误汇总（阶段 AA：`fm_op_threads` 并发上限（默认 2，
  0 = 不限，≤8），超限任务 FIFO 排队、在途结束自动放行、排队可取消；
  非模态任务面板（顶栏「任务」按钮/点击状态栏进度区打开）列出在途+
  排队任务，逐任务暂停/取消；操作含失败项时弹错误汇总窗（虚拟化全量
  列表），「重试失败项」按原 dest/冲突策略/permanent 档重建任务）
- ✅ 复制校验/过滤/压缩暂停（阶段 AB：确认框「复制完成后校验」checkbox，
  worker 复制后逐文件分块比对、不一致记 errors；「高级」折叠区——仅复制
  匹配通配符 + 仅最近 N 天，目录保留只过滤文件；过滤 + Move 为 TC 式部分
  搬运（逐文件删源，任一项失败不整删源）；parser `create_zip` 增 paused
  参数（256KB 分块、块边界轮询），FM 压缩任务暂停/继续解禁。删除字节进度
  早已具备：Delete 循环按 source 粒度累加 done_bytes，trash 无法更细）
- ✅ 校验和 + 属性/时间戳（阶段 AC：右键「校验和…」对话框——后台分块
  单次过同算 CRC32/SHA-1/SHA-256（crc32fast/sha1/sha2 显式声明复用，
  MD5 无依赖不引入），ComboBox 切算法、点击行复制 hex；「验证」模式解析
  md5sum/sfv 校验文件逐条标 ✓/✗/缺失/不支持；右键「修改属性/时间戳…」
  ——只读/隐藏/系统/存档位（platform::file_attr，Windows
  SetFileAttributesW / unix 仅只读位）+ 修改/访问时间设为指定或当前时间
  （filetime 显式声明复用））
- ✅ 分割/合并文件（阶段 AD：OpKind::Split/Merge 走任务队列（进度/暂停/
  取消/错误汇总天然生效）；右键「分割…」按大小（KB/MB/GB）或份数切
  `name.NNN`，已存在分块整批不覆盖、取消删半成品分块；右键「合并…」
  收集连续分块（缺号报错列出），同目录 `.crc`（sfv 单行）存在时流式
  顺带 CRC32 校验）
- ✅ 同步目录（阶段 AE：顶栏/右键「同步目录…」（双栏且目录不同才可用，
  单栏隐藏）——后台递归对比两树（diff_dir_trees：仅左/仅右/左新/右新/
  相同，大小不同按 mtime 新者、大小相同 ≤2s 容差视为相同；目录只参与
  仅左/右判定），结果列表虚拟化、相同项默认隐藏；三方向（左→右/右→左/
  双向）+ 单向可选「删除对侧多余项（回收站）」，执行经 build_sync_plan
  生成 Copy（Overwrite）/Delete 任务走队列；「交换左右」重对比）
- ✅ 按内容比较两个文件（阶段 AF：右键「比较内容…」（选中集恰 2 文件）/
  「比较两栏焦点文件」（双栏两焦点非目录文件）——后台 worker 先二进制
  快比（大小不等直接结论，否则 256KB 分块报首差异偏移），文本判定
  （扩展名或 8KB 无 NUL 嗅探）且 ≤64MB 时行级 diff（自实现 LCS，行数
  乘积超阈值回退仅二进制），单滚动区左右对照（删红/增绿/同无色））

**不做/有意不同（建议保留现状）**
- 删除/移动源进回收站（比 TC 默认安全）
- 压缩包双击进 Archive 视图而非面板内浏览
- FTP/网络邻居、插件体系——超出本项目范围
  （命令行条已由阶段 X 提供基础版；输出捕获/按钮栏不做）
