[package]
name = "openitgo-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Core domain models and reading logic for OpenItGo"

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
